def solution(a, b, c):
    if a == b == c:   # 세 숫자가 모두 같다면
        result = (a+b+c)*(a**2+b**2+c**2)*(a**3+b**3+c**3)
    elif a == b or a == c or b == c:   # 세 숫자 중 어느 두 숫자는 같다면
        result = (a+b+c)*(a**2+b**2+c**2)
    else:   # 세 숫자가 모두 다르다면
        result = a+b+c
    return result
